package auth

// func TestGetSalt(t *testing.T) {
// 	assert := assert.New(t)
// 	saltedHashedPass := getSaltedHashedPassword("password")

// 	assert.Equal(string(bcrypt.CompareHashAndPassword(saltedHashedPass)), "password", "Salt failed")
// }
